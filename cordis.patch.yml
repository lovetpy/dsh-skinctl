# dsh-skinctl bundle patch: inserts its dsh.client row (and the host-side
# route provider) into the web plugin roster.
# Install with: dsh plugin --profile web add dsh-skinctl
- insert:
    - id: ui-skinctl
      name: 'dsh-skinctl'
